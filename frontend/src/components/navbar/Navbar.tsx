import Menu from "components/menu/Menu";

import { NavbarWrapper } from "./Navbar.styled";

const Navbar = () => {
    return (
        <NavbarWrapper>
            <Menu />
        </NavbarWrapper>
    );
};

export default Navbar;
