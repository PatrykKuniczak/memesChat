import styled from "styled-components";
import { ButtonsWrapper, ModalWrapper } from "../GenericModalComponents.styled";

const RemoveAvatarWrapper = styled(ModalWrapper)``;

const RemoveAvatarButtonsWrapper = styled(ButtonsWrapper)``;

export { RemoveAvatarWrapper, RemoveAvatarButtonsWrapper };
