import styled from "styled-components";
import { ButtonsWrapper, ModalWrapper } from "../GenericModalComponents.styled";

const DeleteAvatarWrapper = styled(ModalWrapper)``;

const DeleteAvatarButtonsWrapper = styled(ButtonsWrapper)``;

export { DeleteAvatarWrapper, DeleteAvatarButtonsWrapper };
