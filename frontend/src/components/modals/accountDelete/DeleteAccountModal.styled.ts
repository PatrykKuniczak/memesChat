import styled from "styled-components";
import { ButtonsWrapper, ModalWrapper } from "../GenericModalComponents.styled";

const DeleteAccountWrapper = styled(ModalWrapper)`
    padding: 2rem 3rem;
`;

const DeleteAccountButtonsWrapper = styled(ButtonsWrapper)``;

export { DeleteAccountWrapper, DeleteAccountButtonsWrapper };
